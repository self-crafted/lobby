{
  "entrypoint": "com.github.klainstom.${name.toLowerCase()}.ExtensionMain",
  "name": "${name}",
  "version": "${version}",
  "meta": {
    "github": {
      "release": {
        "account": "KlainStom",
        "repo": "lobby",
        "release": "v${version}"
      }
    }
  }
}