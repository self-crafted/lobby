{
  "entrypoint": "com.github.selfcrafted.${name.toLowerCase()}.ExtensionMain",
  "name": "${name}",
  "version": "${version}",
  "meta": {
    "github": {
      "release": {
        "account": "self-crafted",
        "repo": "lobby",
        "release": "v${version}"
      }
    }
  }
}